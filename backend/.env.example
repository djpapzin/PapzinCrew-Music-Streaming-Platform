# PapzinCrew Backend - .env.example
# Copy this file to .env and fill in the required values.
# Do NOT commit your real .env.

# -----------------------------
# Backblaze B2 (primary storage)
# -----------------------------
# Typical Backblaze S3 endpoint and region for us-east-005:
B2_ENDPOINT=https://s3.us-east-005.backblazeb2.com
B2_REGION=us-east-005
# Your public bucket name
B2_BUCKET=papzincrew-music-djpapzin
# Application Key ID and Secret (from Backblaze)
B2_ACCESS_KEY_ID=REPLACE_ME
B2_SECRET_ACCESS_KEY=REPLACE_ME_SECRET
# Networking/timeouts for B2 client
B2_CONNECT_TIMEOUT=5
B2_READ_TIMEOUT=15
B2_MAX_ATTEMPTS=3
# Upload-specific timeout for B2 put (seconds)
B2_PUT_TIMEOUT=20

# -------------
# App basics
# -------------
# Logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
# Local fallback upload directory (used when B2 is not available or explicitly requested)
UPLOAD_DIR=uploads

# -----------
# Database
# -----------
# If unset, the app uses a local SQLite file in the backend directory.
# Example Postgres URL:
# SQLALCHEMY_DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
# Example SQLite absolute path on Windows (note triple slashes):
# SQLALCHEMY_DATABASE_URL=sqlite:///C:/path/to/papzin_crew.db
SQLALCHEMY_DATABASE_URL=

# -----
# CORS
# -----
# Comma-separated list of allowed origins. Use '*' with caution in production.
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Optional regex alternative for origins (takes effect when set)
ALLOWED_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$
# Dev-only CORS header override used by some proxied stream responses
DEV_CORS_ORIGIN=*

# -----------------------------
# Upload/processing behavior
# -----------------------------
# Enable extra console prints during upload (1=true, 0=false)
ENABLE_UPLOAD_PRINTS=1
# AI cover art generation timeout (seconds)
AI_COVER_TIMEOUT_SECONDS=45.0

# Notes:
# - After changing .env, restart the backend server to apply changes.
# - Keep secrets out of version control. Only commit .env.example.
